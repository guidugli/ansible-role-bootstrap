---
# Generated file. Do not edit directly.
# Source template: {{ template_name }}

dependencies: []

galaxy_info:
  role_name: bootstrap
  namespace: guidugli
  author: guidugli
  company: Creuzo Tech

  description: >-
    Bootstrap Linux hosts with Python and package-manager bindings required by Ansible,
    with first-touch, multi-distribution, and container-aware behavior.
  license: MIT
  min_ansible_version: "2.14"

  platforms:
{% for platform in platforms %}
    - name: {{ platform.name }}
      versions:
{% for version in platform.versions %}
        - {{ version | tojson }}
{% endfor %}
{% endfor %}

  galaxy_tags:
    - bootstrap
    - ansible
    - python
    - packages
    - system
    - setup
    - provisioning
    - linux
    - apt
    - dnf
